(function($, undefined) {
	var ns = ztesoft.namespace("ztesoft.components");

	var NumericStepper = ns.NumericStepper = function(element, options) {

	};

	_.extend(NumericStepper.prototype, Backbone.Events, {

	});
})(window.jQuery);